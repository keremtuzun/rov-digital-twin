# Remaining-gate execution record - 2026-09-04

## Completed locally in response to user approval

1. Recorded Model 1 v2 fallback activation authorization from the actual user instruction.
   This is NOT human label approval or dataset approval; original Model 1 remains blocked.
2. Froze a separate synthetic custom Model 2 protocol, generated/validated fresh S2,
   implemented evidence-gated structural memory and ran 21 controlled experiments.
   Stored metrics/checkpoints were audited; mixed and negative findings are reported.
3. Added a read-only physical validation dossier auditor. It checks prerequisite stages,
   raw-evidence hashes, measured limits and independent review records, but cannot
   authenticate people, certify physical truth, execute hardware tests or authorize deployment.

## Still blocked by missing inputs

Verification at closeout: 172 tests and 11 subtests passed; lint and compilation
passed; the S2 auditor verified all 21 runs without repeated held-out inference.
Two existing Starlette/httpx/anyio deprecation warnings remain. Original S1 artifacts
and frozen evaluation settings are unchanged. Model 1 and physical-dossier checks
return the expected blocked results rather than manufactured success.

**Model 1:** no approved `manifest.csv`, `labels.csv`, `split.csv`, `checksums.sha256`,
`annotation_review.csv` or `dataset_validation.json`. No original visual checkpoints
were provided. Training was not run and no human reviewer identity was fabricated.
The existing two-reviewer packages and submission comparison pipeline remain the path
forward; they were not replaced by AI-generated approvals.

**Physical validation:** no identified controller/vehicle/test rig, qualified operator,
signed risk/acceptance plan, or real test logs were supplied. Unity Editor was not found
at the checked standard installation path. No hardware was energized or commanded.
The empty dossier correctly fails all nine evidence stages. Prior software/static
tests are not reclassified as physical evidence.

**Custom Model 2 promotion:** the model is implemented and experimentally trained,
but OOD interval coverage is approximately 60% for nominal 90% intervals. No claim of
reliable uncertainty, real strength inference, proprietary novelty or production readiness.
See `MODEL2_S2_RESEARCH_RESULTS.md` for the full comparison and next research gate.

## What the user/team must supply

- The two independent reviewed label submissions, adjudication/domain-review evidence,
  and access to the corresponding approved image package (or recovered original Model 1 package).
- Identification of available test hardware and the responsible operator, plus the signed
  test plan/limits; alternatively, existing real test logs and independent review records.

User approval has been used; no further generic approval request is needed for the
completed software work. Missing observations/reviews cannot be manufactured.
Second Brain CLI remains unavailable; this record is saved in the repository instead.
Changes remain local and are not pushed to the public remote.
