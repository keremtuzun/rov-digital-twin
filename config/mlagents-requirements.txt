# Reproducible Unity ML-Agents trainer environment (Python 3.10.x).
# Install the two mlagents packages from the same official release_23 checkout,
# then install these compatibility pins.
setuptools<81
torch==2.1.1
onnx==1.15.0
protobuf>=3.20.3,<=3.20.3
tensorboard>=2.17,<3
